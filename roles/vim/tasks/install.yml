- name: Install dependencies
  become: true
  package: name=libncurses-dev state=present
  when: ansible_distribution == 'Ubuntu'

- name: Clone vim
  git: 
    repo: https://github.com/vim/vim
    dest: /tmp/vim
    depth: 1

- name: Compile vim
  command: make
  args:
    chdir: /tmp/vim

- name: Install vim
  become: true
  command: make install
  args:
    chdir: /tmp/vim

- name: Clone vim config
  git:
    repo: https://github.com/ealang/vimconf
    dest: ~/.vim
